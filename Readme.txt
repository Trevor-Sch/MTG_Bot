Webscaper setup and run

-first Setup a Python environment 

python -m venv venv
venv/script/activate
pip3 install httpx
pip3 install selectolax

- google "My User Agent" for the header to give your webscraper an ID to search the web with
- reads through website to find the deck format, the amount of cards and the name of all the cards in a deck
- imports data to a json file to use later on when we use an API for MTG and combine data sets for training




